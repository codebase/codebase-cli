There's no test file for the functions in src/math.ts. Please write tests for `add` and `multiply` in src/math.test.ts using vitest's `describe` / `it` / `expect` API. Cover the obvious cases (positive numbers, zero, negatives) — three or four tests is enough. After writing the file, run this exact unmasked verification command and name it in your final answer: `test -f src/math.test.ts && grep -q "from ['\\\"]vitest['\\\"]" src/math.test.ts && grep -q '\\badd\\b' src/math.test.ts && grep -q '\\bmultiply\\b' src/math.test.ts && grep -q 'expect(' src/math.test.ts`. Do not wrap the command with `|| true`, `|| echo`, or availability fallbacks.
