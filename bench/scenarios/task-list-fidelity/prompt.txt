Fix every failing inventory behavior in this tiny project, then run `node test.mjs` to verify it. Requirements: `normalizeSku` must trim and uppercase SKUs, `totalCents` must multiply each price by its quantity, and `lowStock` must include items whose quantity is less than or equal to the threshold. Keep a visible task checklist from start to finish. Create separate tasks for inspection, the `normalizeSku` fix, the `totalCents` fix, the `lowStock` fix, and verification; move each task in_progress before doing that specific work and complete it afterward.
